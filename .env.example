VITE_SUPABASE_URL=https://dawqehvpxfgrysmikeei.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_SITE_URL=https://your-app.vercel.app

# Optional: Used only for Hugging Face inference
VITE_HF_API_URL=https://api-inference.huggingface.co/models/
VITE_HF_API_TOKEN=your_huggingface_token

# Optional: Firebase Analytics
VITE_FIREBASE_API_KEY=your_firebase_api_key

# --- EMAIL (NOT Resend browser direct — CORS blocked) ---
# The app sends emails via Supabase Edge Functions or EmailJS.
# 1. Resend: Store RESEND_API_KEY as a Supabase Edge Function Secret (not VITE_ var)
# 2. EmailJS: Set these 3 vars for browser-safe fallback
VITE_EMAILJS_SERVICE_ID=your_emailjs_service_id
VITE_EMAILJS_TEMPLATE_ID=your_emailjs_template_id
VITE_EMAILJS_PUBLIC_KEY=your_emailjs_public_key

# OBSOLETE: Direct browser Resend is blocked by CORS. Do NOT add VITE_RESEND_API_KEY.
# If you already have it, remove it and store the key in Supabase Edge Function Secrets.
